/**
 * Browser entry point — the isomorphic surface plus browser-only utilities.
 * Resolved automatically via the `browser` export condition (and `@ckiller/universe/browser`).
 */
export * from "./index";
export * from "./browser/utils";
