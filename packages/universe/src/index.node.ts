/**
 * Node.js entry point — the isomorphic surface plus Node-only utilities.
 * Resolved automatically via the `node` export condition (and `@ckiller/universe/node`).
 */
export * from "./index";
export * from "./node/utils";
